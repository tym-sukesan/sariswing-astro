---
/**
 * Minimal shared edit toolbar (Discography / Schedule).
 */
interface Props {
  mode?: "view" | "edit";
  editLabel?: string;
  cancelLabel?: string;
  dryRunLabel?: string;
  showDryRun?: boolean;
}

const {
  mode = "view",
  editLabel = "編集",
  cancelLabel = "編集をやめる",
  dryRunLabel = "変更を確認",
  showDryRun = false,
} = Astro.props as Props;
---

<div
  class="gosaki-admin-edit-toolbar"
  data-gosaki-edit-toolbar="true"
  data-mode={mode}
>
  {
    mode === "view" ? (
      <button
        type="button"
        class="gosaki-admin-btn gosaki-admin-btn--primary gosaki-admin-edit-toolbar__btn gosaki-admin-edit-toolbar__btn--primary"
        data-gosaki-edit-start
      >
        {editLabel}
      </button>
    ) : (
      <>
        <button
          type="button"
          class="gosaki-admin-btn gosaki-admin-btn--secondary gosaki-admin-edit-toolbar__btn"
          data-gosaki-edit-cancel
        >
          {cancelLabel}
        </button>
        {
          showDryRun ? (
            <button
              type="button"
              class="gosaki-admin-btn gosaki-admin-btn--secondary gosaki-admin-edit-toolbar__btn"
              data-gosaki-edit-dry-run
            >
              {dryRunLabel}
            </button>
          ) : null
        }
      </>
    )
  }
</div>

<style>
  .gosaki-admin-edit-toolbar {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
    min-width: 0;
    max-width: 100%;
    box-sizing: border-box;
  }
</style>
